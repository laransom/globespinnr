<h1>Photos</h1>

<% @photos.each do |photo| %>
  <ul>
    <li><%= image_tag photo.image_url('thumb') %><br>
    <%= photo.description %></li>
  </ul>
<% end %>

<p><%= link_to 'Add Photo', new_photo_path %></p>
